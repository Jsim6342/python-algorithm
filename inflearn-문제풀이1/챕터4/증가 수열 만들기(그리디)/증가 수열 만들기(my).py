n = int(input())
a = list(map(int,input().split()))

eng = [] # LR 영어 저장

# 암시로 수열 저장
l_start = []
l_start.append(a[0])
a.pop(0)
eng.append('L')
while a:
    l_start[-1]


r_start = []
r_start.append(a[-1])
a.pop()

while a:
    a[0]
# 포기...
